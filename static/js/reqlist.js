/**
 * Created by konglingcun on 3/23/16.
 */

/*
function validate(f) {
    if (f.val().trim() == null | f.val().trim() == "") {
        f.addClass("alert-danger");
        return false;
    }
}

$(function () {
    $("#commit").on("click", function () {
        if ((!validate($("#firsttime"))) | (!validate($("#firstname"))) | (!validate($("#desc"))) | (!validate($("#priority")))) {
            return false;
        }

        $.getJSON("http://10.130.14.54/common/pingback/?project=HmtWebTool&function=ReqList&action=submit&callback=?",
            function (result) {

            });
    })
});*/
